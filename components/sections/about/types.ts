import type { LucideIcon } from "lucide-react";

export interface Highlight {
  title: string;
  description: string;
  icon: LucideIcon;
}

export interface QuickFact {
  label: string;
  value: string;
}
